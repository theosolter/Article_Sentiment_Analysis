const buttonSearch = document.querySelector("#submit")
const loading = document.querySelector("#loading")


loading.addEventListener("submit", () => {
    console.log("OIOIOIOIO")
    loading.classList.remove("hide")
})
